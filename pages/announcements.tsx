import React, { FunctionComponent } from 'react';

interface OwnProps {}

type Props = OwnProps;

const announcements: FunctionComponent<Props> = (props) => {
    return (
        <>
            <h1>announcements</h1>
        </>
    );
};

export default announcements;
